# Example Redis configuration snippet for vector search (Redis Vector or RedisJSON+RedisSearch)
# Adjust modules and settings according to your deployment.

# loadmodule /path/to/redisvector.so
# For Redis with RedisVector, uncomment the loadmodule line above and set appropriate memory limits.

save 900 1
save 300 10
save 60 10000

# maxmemory 4gb
# maxmemory-policy allkeys-lru
