# Tests

This directory contains example unit/integration tests and simple manifest validation scripts used by the handbook CI.

Tests are illustrative and should be extended to cover your repository's real modules and manifests.
